name: Publish Python Package

on:
  workflow_dispatch:
  release:
    types: [created]

jobs:
  build-n-publish:
    name: Build and publish Python 🐍 distributions 📦 to PyPI
    runs-on: ubuntu-latest
    # needs: [test]
    environment: release
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # needed for dynamic versioning to see tags

      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          python-version: "3.11"

      - name: Build source and wheel archives
        run: uv build

      - name: Publish
        uses: pypa/gh-action-pypi-publish@release/v1